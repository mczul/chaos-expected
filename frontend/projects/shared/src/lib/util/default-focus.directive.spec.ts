import { DefaultFocusDirective } from './default-focus.directive';

describe('DefaultFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultFocusDirective();
    expect(directive).toBeTruthy();
  });
});
